import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const NotificationApp = () => {
  return (
    <View>
      <Text>NotificationApp</Text>
    </View>
  );
};

export default NotificationApp;

const styles = StyleSheet.create({});
